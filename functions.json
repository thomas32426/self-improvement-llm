[
    {
        "name": "execute_code",
        "description": "Execute Python code",
        "parameters": {
            "type": "object",
            "properties": {
                "code": {
                    "type": "string",
                    "description": "Text of entire Python script."
                },
                "timeout": {
                    "type": "integer",
                    "description": "Timeout duration in seconds"
                }
            },
            "required": ["code", "timeout"]
        }
    },
    {
        "name": "web_search",
        "description": "Search Duck Duck Go",
        "parameters": {
            "type": "object",
            "properties": {
                "search_query": {
                    "type": "string",
                    "description": "Text query sent to duckduckgo.com"
                },
                "max_results": {
                    "type": "integer",
                    "description": "Maximum number of results to return"
                }
            },
            "required": ["search_query", "max_results"]
        }
    }
]